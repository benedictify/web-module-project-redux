  const initialState = {
	favorites: [],
	displayFavorites: true
}

const favorites = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state; 
	}
}

export default favorites;
